'use strict';

/**
 * spotify-gift-card service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::spotify-gift-card.spotify-gift-card');
