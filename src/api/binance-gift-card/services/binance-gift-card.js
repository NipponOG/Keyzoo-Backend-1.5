'use strict';

/**
 * binance-gift-card service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::binance-gift-card.binance-gift-card');
